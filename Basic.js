// Basic
// naming abstraction
const a = 5;
const b = 2;
a / b; // 2.5
a % b; // 1
x < 1 ? a + 1: a + 5;
// predicate ? consequent statement: alternate consequent statement
// Only evaluate the one that is selected.
// But the statement itself should have right syntax
// function abstraction
function c(/* parameter*/){
    // argument
}
c(); // function call;
// compounds are functions that I made
// funcitons are functions imported or origninally made, primitive
